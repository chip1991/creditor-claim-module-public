- [x] 权限中心信息架构清晰：组织/角色/用户三入口可达
- [x] 前端组织管理支持搜索/分页，并可同步组织树且展示结果
- [x] 前端角色管理支持搜索/分页，角色详情支持菜单权限、接口权限、数据范围三件套配置并可保存
- [x] 前端用户管理支持搜索/分页，支持分配角色并保存，支持用户启用/禁用
- [x] 后端提供 /api/iam 与 /api/rbac 接口契约并与前端对齐（records/total 等结构一致）
- [x] 数据范围合并策略按 spec（最宽范围优先）生效
- [x] 审计覆盖：组织同步、角色权限变更、用户角色分配、用户启停均写入 audit_log
- [x] 过渡期兼容策略明确：/v1 保留只读兼容或返回迁移提示
- [x] 最小回归通过：三页核心流程可手工走通（同步→搜索→配置→分配→启停）
- [x] 组织管理页不再以同步为主操作，支持组织新增、编辑、删除，且删除拦截规则生效
- [x] 角色管理页不再以同步为主操作，支持角色新增、编辑、删除，且删除已分配角色时有中文提示
- [x] 用户管理页不再以同步为主操作，支持用户新增、编辑、删除，并保留分配角色与启停
- [x] 后端 `/api/iam` 提供组织/角色/用户 CRUD 接口，响应结构与前端表单流程对齐
- [x] 审计覆盖组织/角色/用户新增、编辑、删除等主数据维护动作
- [x] `npm run build` 通过，且前后端分离场景下开发服务已按要求重启
- [x] 运行库中 `permission` 存在 `data:import`、`data:read`，且 admin 角色自动拥有这两项权限
- [x] 使用 `satoken=dev-bypass` 时，认证命中稳定的 admin 用户，不再受“第一个启用用户”排序影响
- [x] 原始数据页面在列表加载失败或上传失败时会展示明确中文错误提示，而非静默无反馈
- [x] 原始数据上传接口在权限不足时返回 403 且文案可理解，在权限满足时可成功创建导入任务
- [x] 使用真实 Excel 文件自测上传通过，批次记录可在列表页查询到
