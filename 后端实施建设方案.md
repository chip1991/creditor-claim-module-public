# 物业业主投诉根因分析与闭环治理AI智能体（Python后端）实施建设方案

本文档将《物业业主投诉根因分析与闭环智能Agent需求文档》中涉及的全量后端能力，整理为可落地实施的 Python 技术栈建设方案。目标是指导从 0 到 1 建设可演示、可扩展、可审计的后端系统。

---

## 1. 建设目标与成功标准

### 1.1 建设目标
- 支撑端到端闭环链路：投诉数据导入 → 清洗 → AI分析 → 生成整改工单 → 整改提交 → 核验 → 满意度校验 → 闭环 → 看板与报告同步更新。
- 支撑“互动性”：长耗时任务可观测进度（导入、清洗、分析、报告、导出等），必要时支持流式返回（SSE）。
- 满足权限与审计：角色/数据范围/敏感信息脱敏与明文开关、关键操作可追溯（审计日志）。
- 满足可配置：分类字典、根因知识库、规则配置、权限配置可动态调整，并影响业务行为。

### 1.2 MVP验收标准（与需求文档 9.1/9.2 对齐）
- 场景A：标准闭环（投诉→整改→核验→满意度→闭环）完整可演示，且每一步产生审计日志。
- 场景B：匹配失败修复（满意度数据匹配失败→人工修复→补齐校验）完整可演示。
- 场景C：核验不通过退回 + 二次整改 + 满意度不通过自动退回完整可演示。
- 关键状态机严格符合需求文档 12.x 定义。
- 指标口径一致：看板、报告、问答取数全部复用同一口径计算逻辑（需求文档 15.x）。

---

## 2. 范围界定

### 2.1 本期范围（全量后端能力）
- 认证与权限（RBAC + 数据范围）
- 数据管理中心：导入/校验/清洗/关联/修复/导出
- 投诉AI分析：分析、人工修正、重新分析、证据片段、置信度策略
- 整改闭环：工单生成、流转、预警/升级、核验、满意度校验、自动退回、强制闭环
- 看板：指标聚合、筛选、钻取明细
- 智能问答：自然语言→结构化查询→取数→结论生成（含流式交互）
- 自动化报告：手动生成/自动生成、详情查看、导出
- 系统配置：分类/知识库/权限/规则配置与版本化
- 审计与通知：全量审计日志、通知记录与已读

### 2.2 明确不做/延后项（可选）
- 语音输入（前端/客户端能力更强，后端只提供可选 ASR 接口，不作为MVP强依赖）
- 多租户隔离（如无多租户需求，可延后；若赛事需要可用 tenant_id 预留字段）
- 复杂BI大屏组件（后端只提供指标与图表数据）
- 工作日历的复杂节假日库（MVP可先自然日 + “未配置提示”，后续再精细）

---

## 3. 总体架构与技术选型（Python）

### 3.1 架构形态
- MVP阶段：单体应用（FastAPI）+ 异步任务（Celery）+ PostgreSQL + Redis + 对象存储（MinIO/S3），统一部署，降低复杂度。
- 扩展阶段：按业务域拆分（数据域/分析域/工单域/报表域/系统配置域），逐步演进为多服务。

### 3.2 推荐技术栈
- API：FastAPI（异步、类型、OpenAPI）
- ORM/迁移：SQLAlchemy 2.x + Alembic
- DB：PostgreSQL
- 缓存/队列：Redis
- 异步任务：Celery（含 Celery Beat 定时）
- 文件处理：openpyxl（Excel）、pandas（可选，注意内存）
- 导出：ReportLab/WeasyPrint（PDF）、python-docx（Word，可选）
- LLM：独立封装 `llm_service`（支持多模型与配置）
- 实时交互：SSE（优先），WebSocket（必要时）
- 可观测：structlog/loguru + Prometheus metrics + OpenTelemetry（可选）

---

## 4. 后端工程组织（建议目录结构）

```
backend/
  app/
    main.py
    core/
      config.py
      logging.py
      security.py          # token解析、权限校验
      exceptions.py
      middleware.py        # 请求ID、审计上下文注入
    db/
      session.py
      base.py
      migrations/          # Alembic
    models/                # SQLAlchemy models
    schemas/               # Pydantic schemas（请求/响应）
    api/
      v1/
        router.py
        auth.py
        data.py
        analysis.py
        workorder.py
        dashboard.py
        qa.py
        report.py
        system_config.py
        notification.py
        task.py
    services/
      data_import_service.py
      data_clean_service.py
      data_link_service.py
      analysis_service.py
      workorder_service.py
      satisfaction_service.py
      dashboard_service.py
      qa_service.py
      report_service.py
      audit_service.py
      notification_service.py
      rule_service.py
      file_service.py
      llm_service.py
    tasks/
      celery_app.py
      workers.py            # 任务注册
      schedules.py          # beat schedule
    utils/
      time_utils.py
      masking.py
      id_gen.py
      pagination.py
  tests/
  docker/
  pyproject.toml
  README.md
```

说明：
- `api` 只做参数校验、鉴权、调用 service；业务逻辑在 `services`。
- `tasks` 负责耗时操作（导入、清洗、分析、报告、导出、预警扫描）。
- `audit_service` 在 service 层统一落审计，禁止散落各处。

---

## 5. 数据模型设计（核心实体与表）

> 与需求文档 10.1 对齐：DataRecord、ComplaintAnalysis、RectificationWorkOrder、Category、RootCauseKB、Department/User、RuleConfig、AuditLog、NotificationLog、Report。

### 5.1 通用字段规范
- `id`：全局唯一（UUID/雪花ID均可）
- `created_at / updated_at`
- `created_by / updated_by`（用户ID）
- `deleted_at`（软删除可选）
- `tenant_id`（可选预留）

### 5.2 数据记录（data_record）
用于承载投诉/满意度/外呼考核三类数据，保留原始内容与清洗内容。
- 核心字段（示例）：
  - `data_type`（枚举：业主进线投诉数据/400外呼满意度数据/外呼回访考核数据）
  - `work_order_no`（关联工单号）
  - `raw_content`（原始数据内容，必须保留）
  - `clean_content`（清洗后内容，可空）
  - `owner_name / owner_phone / building_room`（可空，脱敏策略见 9.4）
  - `call_time / visit_time / agent / duration`（按数据类型适配）
  - `process_status`（待清洗/已清洗/已分析/已关联/匹配失败）
  - `source_file_id`（导入文件引用）

### 5.3 投诉分析（complaint_analysis）
建议与 `work_order_no` 唯一关联，也可一对多保留历史版本。
- `analysis_id`
- `work_order_no`
- `category_lv1 / category_lv2`
- `root_surface / root_direct / root_deep`
- `responsible_dept`
- `risk_level`（高/中/低）
- `is_repeated`（bool）
- `confidence`（0~1）
- `evidence_snippets`（JSON：证据文本+原因）
- `suggested_requirement / suggested_deadline`
- `model_version / analyzed_at`
- `manual_overrides`（JSON：人工修正字段与来源）
- `status`（待分析/已分析/需人工确认等）

### 5.4 整改工单（work_order）
围绕需求文档 12.x 状态机落库，避免纯内存/前端状态。
- `work_order_id`
- `work_order_no`
- `analysis_id`
- `department / assignee`
- `requirement`（整改要求）
- `deadline`（整改时限）
- `rectify_status`（待生成工单/待整改/整改中/待核验/已闭环/整改失败）
- `verify_status`（待核验/核验通过/核验不通过）
- `close_status`（未闭环/已闭环/逾期未闭环）
- `warning_status`（正常/即将逾期/已逾期）
- `satisfaction_status`（待校验/校验通过/校验不通过）
- `result_text`（整改结果）
- `force_close_reason`（强制闭环原因）
- `returned_times`（退回次数）

### 5.5 满意度记录（satisfaction_record）
支持 400外呼满意度与外呼考核两类来源（需求文档 11.4）。
- `source_type`（400外呼/外呼考核）
- `work_order_no`（可空：考核数据可能无法关联）
- `score`（1-10）
- `result_enum`（满意/基本满意/不满意；由规则映射）
- `raw_content`
- `visit_time / agent`

### 5.6 配置类
建议统一 `config` 表 + 分 key 管理，并提供版本化。
- `config_key`（category/knowledge/permission/rules 等）
- `config_value`（JSON/Text）
- `version`
- `enabled`

### 5.7 审计日志（audit_log）
严格对齐需求文档 13.3。
- `entity_type / entity_id`
- `action`（导入/清洗/关联/分析/修正/生成工单/提交整改/核验/退回/闭环/导出/配置变更等）
- `operator_id / operator_role / operator_dept`
- `before_value / after_value`（JSON）
- `reason`（退回原因、强制闭环原因等）
- `source`（页面/接口/定时任务）
- `created_at`

### 5.8 通知（notification）
用于消息中心与预警推送。
- `type`（预警/升级/系统）
- `title / content`
- `receiver_user_id`
- `read_flag`
- `created_at`

### 5.9 任务表（task）
承载所有长任务的统一进度与结果引用。
- `task_type`（import/clean/link/analysis/report/export/warning_scan）
- `status`（PENDING/RUNNING/SUCCESS/FAILED）
- `progress`（0~100）
- `message`
- `result_ref`（file_id/entity_id/json）
- `created_by`
- `created_at`

---

## 6. 关键业务流程与状态机落地

### 6.1 数据处理状态机（需求文档 12.1）
- 待清洗 → 已清洗 → 已分析（仅投诉数据） → 已关联
- 匹配失败：关联失败进入，允许人工修复后回到已关联

落地建议：
- 每次状态变化都写 `audit_log` 与过程日志（clean_log/link_log）。
- 状态变化由 service 控制，禁止直接 SQL 更新。

### 6.2 工单状态机（需求文档 12.2/12.3）
建议实现“主状态+子状态”的有限状态机（FSM），并强制校验允许的转移。
- 主状态（rectify_status）：待生成工单/待整改/整改中/待核验/已闭环/整改失败
- 子状态：核验状态、闭环状态、预警状态、满意度校验状态

关键点：
- 核验通过后进入满意度校验阶段；是否允许无满意度数据闭环由规则决定（默认不允许，需求 12.6）。
- 自动退回：满意度校验不通过触发回到整改中，增加退回次数，并记录触发规则命中。

### 6.3 重复投诉判定（需求文档 11.2）
规则：同一联系电话（缺失则楼栋房号）+同一投诉二级分类，30天内>=2次。
落地建议：
- 分析阶段计算并写入 `complaint_analysis.is_repeated`。
- 时间窗与阈值次数写入规则配置。

### 6.4 外呼回访考核数据现实约束（需求文档 11.3）
落地策略：
- 默认不强行与工单闭环绑定：进入“满意度与服务评价分析池”，参与看板与主题分析。
- 如要纳入闭环，需要额外可关联主键（业主ID/房产ID/未脱敏手机号映射/工单号）与配置开关。

---

## 7. API 设计（建议统一规范）

### 7.1 统一响应格式
- 成功：
  - 列表：`{ "records": [...], "total": 0, "page": 1, "size": 20 }`
  - 详情：`{ ... }`
  - 操作：`{ "success": true, "msg": "操作成功", "data": {...可选...} }`
- 失败：
  - HTTP 4xx/5xx + `{ "code": "...", "msg": "中文错误信息", "detail": {...} }`

### 7.2 与前端现有路径对齐（已在前端出现）
> 建议后端优先实现这些路径以减少前后端改动成本：
- 数据中心：
  - `GET /data/page`
  - `GET /data/detail?id=`
  - `POST /data/import`（建议改为上传文件；若保持无参则视为“触发导入任务”，但需要明确数据来源）
  - `POST /data/clean`
  - `POST /data/link`
  - `GET /data/export`
- 分析：
  - `GET /analysis/page`
  - `GET /analysis/detail?id=`
- 工单：
  - `GET /workorder/page`
  - `GET /workorder/detail?id=`
  - `POST /workorder/create`
  - `POST /workorder/submit`
- 看板：`GET /dashboard/overview`
- 问答：`POST /qa/ask`（增强：`GET /qa/stream`）
- 报告：`GET /report/page`、`GET /report/detail?id=`
- 系统配置：`/system/*/get`、`/system/*/save`
- 通知：`GET /notification/list`、`POST /notification/read-all`
- 退出：`POST /logout`

### 7.3 长任务与互动（建议新增）
- `POST /task/create`（内部）
- `GET /task/{id}`：查询任务状态
- `GET /task/{id}/stream`：SSE 推送进度（导入/清洗/分析/报告/导出）
- `GET /qa/stream`：问答流式输出（SSE）

---

## 8. 长耗时任务与SSE互动方案（核心）

### 8.1 为什么必须异步化
- Excel 导入可能包含大量行，且需要行级校验与错误明细返回。
- 清洗、关联、AI分析、报告生成、导出都可能耗时，且需要可观测与可取消。

### 8.2 统一任务中心模式
1. API 接到请求后创建 `task` 记录，返回 `task_id`
2. Celery worker 执行任务，持续更新 `task.progress/message/status`
3. 前端可轮询 `GET /task/{id}` 或订阅 `GET /task/{id}/stream`（SSE）

### 8.3 SSE消息格式（建议）
```
event: progress
data: {"taskId":"...","progress":30,"message":"正在清洗第 1200/5000 行","status":"RUNNING"}

event: done
data: {"taskId":"...","status":"SUCCESS","result":{"imported":4800,"failed":200}}
```

### 8.4 失败与重试策略
- 网络/下游服务失败：任务状态标记 FAILED，记录 error_code + msg（中文）
- LLM 调用：超时重试（指数退避），超过次数后失败并可手动重试
- 幂等：同一工单号重复导入/重复生成工单必须有幂等策略（默认拒绝并提示冲突行）

---

## 9. 安全、权限、脱敏与审计（必须项）

### 9.1 认证（Token）
建议 MVP 使用 JWT（或与 Sa-Token 兼容的 header `satoken`），后续可对接 IAM。
- 统一从 header 读取：`satoken`
- 401 策略：返回 401，前端会跳转登录

### 9.2 授权（RBAC）
严格对齐需求文档 13.1/13.2：
- 客服专员：可导入、清洗、关联、修正；不可核验闭环
- 执行人员：仅本部门工单；可提交整改；不可配置规则
- 部门管理者：可核验/退回/改派/催办/处理升级
- 集团管理层：只读看板/报告（导出可配置）
- 系统管理员：全量权限（含系统配置）

### 9.3 数据范围（Data Scope）
- 工单查询默认按用户部门过滤（执行人员/管理者）
- 看板按权限返回可见数据范围（管理层全局只读）

### 9.4 脱敏与明文查看（需求文档 13.4）
- 默认脱敏：手机号、楼栋房号、原文（展示与导出）
- 明文查看：
  - 仅管理员可配置开关
  - 仅具备“明文查看权限”的角色生效
  - 每次明文查询写入审计日志

### 9.5 审计（需求文档 13.3）
审计对象必须覆盖：
- 导入/删除、清洗、关联、AI重新分析、人工修正、生成工单、改派、修改时限、提交整改、核验、满意度校验、自动退回、闭环、导出、配置变更

---

## 10. LLM与人机协作（需求文档 14.x 落地）

### 10.1 LLM调用封装
- 统一输入输出 schema（Pydantic）
- 强制记录：
  - prompt 模板版本、模型版本、输入摘要（脱敏）、输出、置信度、证据片段、耗时、错误信息

### 10.2 低置信度策略（需求文档 14.2）
- `confidence < 阈值`：
  - 标记“需人工确认”
  - 禁止自动批量生成工单或进入待生成工单状态
- `confidence >= 阈值`：
  - 允许一键生成工单、批量处理

### 10.3 人工修正策略
默认不被自动覆盖；只有用户选择“重新分析并覆盖”才更新。

---

## 11. 问答与报告的取数口径一致性（关键）

### 11.1 指标聚合服务（单一口径）
建议建立 `metrics_service`：
- 所有看板、报告、问答取数统一走同一套函数/SQL视图
- 避免一个口径用于看板，另一个口径用于报告导致数据不一致

### 11.2 问答（QA）两段式
- Planner：把自然语言解析成结构化 DSL（时间范围、指标、维度、过滤条件）
- Executor：执行查询 → 返回表格/序列数据
- LLM 生成结论：结论必须引用“证据数据”，避免空口结论

---

## 12. 预警、升级、工作日历与定时任务（需求文档 12.4）

### 12.1 定时扫描
定时任务（每 N 分钟/小时）：
- 计算 `deadline - now`，更新预警状态：正常/即将逾期/已逾期
- 已逾期超过 N 个工作日 → 升级通知至部门负责人

### 12.2 工作日口径
- MVP：若未配置节假日历，则按自然日计算，并在界面/接口返回 `calendarMode=natural` 提示
- 后续：维护 `holiday_calendar` 表支持节假日配置

---

## 13. 测试、验收与联调

### 13.1 测试策略
- 单元测试：规则引擎、状态机转移、脱敏、重复投诉判定、满意度映射
- 集成测试：导入→清洗→分析→工单→核验→满意度→闭环全链路
- 契约测试：API 请求/响应 schema 校验（避免前后端联调反复）

### 13.2 验收用例
严格按需求文档 9.2 场景A/B/C 设计自动化用例与手工演示脚本。

---

## 14. 部署、环境与运维（建议）

### 14.1 环境划分
- dev：本地/联调
- test：验收演示
- prod：正式（如需要）

### 14.2 配置项（环境变量）
- DB/Redis/对象存储连接
- LLM Key/Endpoint（严禁写入日志）
- 规则默认值与开关（置信度阈值、满意度阈值、预警阈值等）
- 脱敏开关与明文查看权限策略

### 14.3 可观测
- 请求日志带 request_id
- 任务执行日志带 task_id
- 关键指标：导入耗时、分析耗时、失败率、预警数量、闭环时长

---

## 15. 分阶段交付计划（从MVP到全量）

### 15.1 阶段1：打通闭环演示（最高优先级）
- 数据导入（投诉/满意度）+ 行级校验
- 清洗 + 关联
- AI分析 + 生成工单
- 工单提交整改 + 核验 + 满意度校验 + 闭环
- 审计日志全覆盖

### 15.2 阶段2：异常链路与规则完整化
- 匹配失败修复、退回与二次整改、满意度不通过自动退回
- 预警与升级定时任务
- 工作日历（可先自然日）

### 15.3 阶段3：看板/问答/报告
- 指标聚合服务统一口径
- 看板全模块接口
- 问答（planner+executor）+ SSE流式
- 报告生成与导出（PDF优先）

### 15.4 阶段4：系统配置与治理
- 分类/知识库/权限/规则配置完善（版本化、导入导出、同步）
- 安全强化（明文查看开关、审计增强）

---

## 16. 风险与对策

### 16.1 LLM 输出不稳定
- 强约束结构化输出（JSON schema），失败重试与降级（返回需人工确认）
- 记录证据片段与模型版本，支持复现

### 16.2 Excel导入质量差与脏值复杂
- 行级错误明细 + 清洗日志
- 脏值映射表可配置并版本化

### 16.3 口径不一致导致看板/报告/问答冲突
- 单一指标聚合服务 + 统一口径SQL/视图

### 16.4 权限与脱敏遗漏
- “默认脱敏、最小权限”策略
- 明文查看必须审计

---

## 17. 与前端联调说明（当前前端已对齐的接口清单）
- 数据：/data/page /data/detail /data/import /data/clean /data/link /data/export
- 分析：/analysis/page /analysis/detail
- 工单：/workorder/page /workorder/detail /workorder/create /workorder/submit
- 看板：/dashboard/overview
- 问答：/qa/ask
- 报告：/report/page /report/detail
- 配置：/system/*/get /system/*/save
- 通知：/notification/list /notification/read-all
- 退出：/logout

建议后端优先实现上述路径，减少前后端改动。

