# 物业业主投诉根因分析与闭环智能Agent需求文档

## 文档说明

本文档为腾讯云AI智能体争霸赛参赛项目专属需求文档，严格贴合赛事规则，基于用户现有400投诉进线数据、400外呼满意度回访数据设计，突出AI Agent核心能力，明确全量功能、页面、字段与枚举值，同时标注开发优先级，保障快速落地Demo。

## 一、项目核心定义

### 1.1 项目名称

物业业主投诉根因分析与闭环治理AI智能体

### 1.2 核心使用角色

| 角色 | 核心使用场景 |
| --- | --- |
| 客服专员 | 投诉数据导入、AI分析结果校验、工单跟进 |
| 部门执行人员 | 接收整改任务、提交整改结果、处理预警工单 |
| 项目/部门管理者 | 查看本部门工单进度、处理超时预警、审核整改结果 |
| 集团管理层 | 查看全量数据看板、分析报告、决策优化 |
| 系统管理员 | 系统配置、权限管理、知识库维护 |

### 1.3 参赛产品适配方案（赛事硬性要求）

| 腾讯云参赛产品 | 对应核心应用场景 |
| --- | --- |
| CodeBuddy（AI代码助手） | 1. 开发AI语义解析、根因分析、智能问答核心代码；2. 快速实现LLM能力调用与模型微调；3. 完成数据清洗、统计分析相关代码开发 |
| WorkBuddy（AI工作助手） | 1. 编排投诉整改全流程自动化工作流，实现Agent自主流转；2. 配置超时预警、催办、升级等自动化规则；3. 自动生成标准化整改工单、分析报告 |
| ClawPro（企业级AI平台） | 1. 搭建400双数据源的企业级数据底座，实现数据接入、存储、清洗；2. 构建物业投诉专属知识库与根因分析模型；3. 实现可视化数据看板、多维度数据分析 |

## 二、项目背景

物业行业业主投诉核心通过400电话进线反馈，后续由400部门完成整改后满意度外呼回访，企业已积累海量业主投诉原始记录、400外呼满意度调查明细两类核心业务数据。

当前业务存在核心痛点：

1. 投诉处理停留在“记录-流转”浅层环节，无法自动化深挖问题根本原因，同类问题重复投诉频发，治标不治本；
2. 投诉数据与满意度回访数据完全割裂，无法通过业主真实反馈校验整改效果，整改敷衍、虚假闭环问题突出；
3. 整改流程无标准化闭环管控，整改进度难追踪、超时无预警，业主投诉石沉大海，满意度持续走低；
4. 投诉数据无体系化分析能力，管理层无法快速获取数据洞察，无法通过数据驱动服务流程优化，管理决策滞后。

为解决上述痛点，依托大赛指定三大腾讯云AI产品，打造业主投诉根因分析与闭环治理AI智能体，实现投诉数据自动化处理、根因智能定位、全流程自主闭环管控、满意度反向校验、数据化决策支撑，全面提升业主满意度、降低重复投诉率，同时打造高落地性、高行业竞争力的参赛作品，冲击赛事一等奖。

## 三、项目目标

### 3.1 量化业务目标

1. 投诉根因分析准确率≥90%，替代人工80%的投诉分类与分析工作量；
2. 整改工单自动生成率100%，整改闭环时长平均缩短50%；
3. 同类问题重复投诉率下降30%以上；
4. 业主投诉处理满意度提升20%以上；
5. 管理报告生成效率提升100%，替代人工全量报告撰写工作。

### 3.2 产品能力目标

1. 实现400双数据源自动接入、清洗、结构化处理，打通数据壁垒，数据关联匹配准确率100%；
2. 打造物业专属投诉分类体系与根因分析知识库，实现投诉问题智能分类、三层级根因自动化深度拆解；
3. 实现AI Agent全流程自主闭环管控：自动生成整改工单→自主流转→超时预警→整改核验→满意度反向校验→不合格工单自动退回；
4. 搭建多维度可视化数据看板与自动化报告体系，为管理层提供数据决策支撑；
5. 实现Agent自学习迭代，基于满意度校验结果持续优化根因分析模型，形成AI能力闭环。

### 3.3 赛事参赛目标

1. 完成可完整演示的AI智能体Demo，覆盖核心业务全流程；
2. 突出行业独家数据源、业务闭环、AI深度应用三大核心优势，满足赛事四大评审维度要求；
3. 打造可复制、可商业化的物业行业AI解决方案，冲击赛事全国总决赛一等奖。

## 四、项目成果

1. 完整上线物业业主投诉根因分析与闭环治理AI智能体，实现核心业务流程全自动化、智能化运转；
2. 沉淀《物业行业投诉分类标准》《物业投诉根因分析知识库》，形成行业专属AI资产；
3. 输出可量化的业务价值成果，实现投诉处理效率、业主满意度双提升，重复投诉率显著下降；
4. 产出全套参赛交付物：可演示Demo、产品需求文档、路演PPT、操作手册、案例数据；
5. 形成标准化的物业投诉闭环治理解决方案，可快速复制至全行业多物业项目，具备商业化落地能力。

## 五、AI智能体核心能力（赛事核心评审重点）

本项目核心为AI智能体，而非传统管理系统，核心实现四大智能能力：

1. 自主感知能力：自动接入400投诉数据，实时感知新增投诉、重复投诉、高风险投诉，无需人工触发；
2. 自主决策能力：自动完成投诉分类、根因拆解、责任部门定位、风险评级，自主决策整改要求、整改时限、处理人；
3. 自主执行能力：自动生成整改工单、自动下发任务、自动超时预警、自动催办、自动联动满意度数据校验整改效果、自动退回不合格工单，全流程无需人工干预；
4. 自学习迭代能力：基于人工修正结果、满意度校验结果，自动优化根因分析模型、分类规则、整改流程，越用越精准。

## 六、核心功能模块总览

| 模块序号 | 模块名称 | 核心功能 | 开发优先级 |
| --- | --- | --- | --- |
| 模块1 | 数据管理模块 | 400投诉数据、满意度数据接入、清洗、关联匹配、管理 | 最高（MVP必做） |
| 模块2 | AI智能分析模块 | 投诉智能分类、三层级根因分析、责任定位、风险评级、重复投诉识别 | 最高（MVP必做） |
| 模块3 | 整改闭环智能管控模块 | 工单自动生成、流程自主流转、超时预警、整改核验、满意度反向校验 | 最高（MVP必做） |
| 模块4 | 可视化数据看板模块 | 核心指标概览、多维度数据分析、趋势监控、热力分布 | 最高（MVP必做） |
| 模块5 | AI智能问答模块 | 自然语言数据查询、智能问答、结论输出 | 高（加分项） |
| 模块6 | 自动化报告模块 | 日报/周报/月报自动生成、查看、导出 | 中（加分项） |
| 模块7 | 系统配置模块 | 分类配置、根因知识库配置、部门权限配置、规则配置 | 中（MVP基础配置） |

## 七、系统页面详细设计（含全量字段、枚举、功能）

### 标注说明

- 【MVP核心页面】：赛事Demo必做页面，优先开发，保障核心流程可演示
- 【扩展加分页面】：MVP完成后开发，用于提升赛事竞争力
- 所有列表、表单、筛选字段100%明确，枚举值完整覆盖

### 【MVP核心页面1：登录页】

#### 页面核心功能

实现不同角色账号登录、权限校验，是系统唯一入口

#### 核心字段与表单

| 表单字段 | 字段类型 | 字段规则 |
| --- | --- | --- |
| 账号 | 文本输入框 | 必填，支持手机号/工号登录 |
| 密码 | 密码输入框 | 必填，加密传输 |
| 记住账号 | 单选框 | 可选，本地缓存账号 |

#### 页面操作功能

1. 登录：账号密码校验通过后，根据角色权限跳转对应首页
2. 忘记密码：通过手机号验证码重置密码

#### 权限规则

- 不同角色登录后，仅展示对应权限的菜单与功能
- 无权限页面禁止直接访问跳转

### 【MVP核心页面2：数据管理中心页】

#### 页面核心功能

400投诉数据、满意度数据的导入、清洗、关联、管理，是系统数据底座入口

#### 列表全字段

| 字段名称 | 字段类型 | 枚举值/规则 |
| --- | --- | --- |
| 数据ID | 文本 | 系统自动生成，唯一标识，不可编辑 |
| 数据类型 | 枚举单选 | 【业主进线投诉数据、400外呼满意度数据、外呼回访考核数据】 |
| 关联工单号 | 文本 | 唯一工单编号，投诉与满意度数据通过工单号关联，必填 |
| 业主基础信息 | 组合字段 | 业主姓名、楼栋房号、联系电话，非必填 |
| 400通话核心信息 | 组合字段 | 通话时间、通话时长、接待坐席，必填 |
| 原始数据内容 | 长文本 | 投诉原始描述/满意度回访原始记录，必填 |
| 数据处理状态 | 枚举单选 | 【待清洗、已清洗、已分析、已关联、匹配失败】 |
| 上传时间 | 时间 | 系统自动记录，不可编辑 |
| 操作人 | 文本 | 系统自动记录，不可编辑 |

#### 筛选条件（全量可组合查询）

1. 数据类型（多选）
2. 数据处理状态（多选）
3. 上传时间范围（起止时间）
4. 关联工单号（精准搜索）
5. 楼栋房号（精准/模糊搜索）
6. 业主姓名/联系电话（模糊搜索）

#### 排序规则

1. 默认按上传时间倒序排列
2. 支持按通话时间、工单号正序/倒序排列

#### 分页功能

1. 支持分页展示，默认每页20条
2. 支持自定义每页展示条数（10/20/50/100条）
3. 支持页码快速跳转、首页/末页直达

#### 页面核心操作功能

1. 新增/导入：支持Excel模板批量导入投诉数据、满意度数据，导入前自动校验格式，缺失必填字段提示报错
2. 一键清洗：选中单条/批量数据，自动清洗无效语气词、冗余话术，结构化提取核心字段
3. 自动关联：选中单条/批量数据，通过工单号自动匹配投诉数据与对应满意度数据，匹配失败标记提示
4. 查看详情：跳转至数据详情页，查看原始数据、清洗结果、关联信息
5. 编辑：手动修改清洗后的字段、关联工单号，修正系统处理误差
6. 删除：单条/批量删除无效、重复数据，删除前二次确认
7. 导出：导出筛选后的全量列表数据，支持Excel格式
8. 查询：基于筛选条件组合精准查询目标数据

#### 扩展功能

1. 清洗日志：查看每条数据的清洗记录、处理结果、异常说明
2. 导入模板下载：提供标准化Excel导入模板，保障数据格式统一
3. 匹配失败处理：针对工单号匹配失败的数据，支持手动关联匹配

### 【MVP核心页面3：投诉AI分析列表页】

#### 页面核心功能

展示AI智能体分析后的全量投诉数据，是核心功能页面，支持查看分析结果、发起整改流程

#### 列表全字段

| 字段名称 | 字段类型 | 枚举值/规则 |
| --- | --- | --- |
| 分析ID | 文本 | 系统自动生成，唯一标识，不可编辑 |
| 关联工单号 | 文本 | 关联对应投诉工单，唯一，不可编辑 |
| 业主基础信息 | 组合字段 | 业主姓名、楼栋房号、联系电话 |
| 投诉核心信息 | 组合字段 | 投诉时间、投诉内容摘要、通话坐席 |
| 投诉一级分类 | 枚举单选 | 【设施设备类、物业服务类、收费争议类、环境绿化类、安全管理类、邻里纠纷类、其他类】 |
| 投诉二级分类 | 枚举单选 | 对应一级分类的细分枚举，详见文末枚举汇总 |
| 根因分析结果 | 组合字段 | 表层问题、直接原因、深层管理根因，AI自动生成 |
| 责任部门 | 枚举单选 | 【工程部、保洁部、客服部、秩序部、财务部、项目管理部、地产维保部】 |
| 风险等级 | 枚举单选 | 【高、中、低】 |
| 重复投诉标记 | 布尔值 | 【是、否】，同一业主/同一楼栋同类问题30天内2次及以上投诉标记为是 |
| 整改状态 | 枚举单选 | 【待生成工单、待整改、整改中、待核验、已闭环、整改失败】 |
| 满意度核心信息 | 组合字段 | 满意度评分（1-10分）、满意度结果【满意、基本满意、不满意】 |
| 分析时间 | 时间 | 系统自动记录AI分析时间，不可编辑 |

#### 筛选条件（全量可组合查询）

1. 投诉时间范围（起止时间）
2. 投诉一级/二级分类（多选）
3. 责任部门（多选）
4. 风险等级（多选）
5. 整改状态（多选）
6. 满意度结果（多选）
7. 重复投诉标记（单选）
8. 楼栋房号、工单号、业主姓名（精准/模糊搜索）

#### 排序规则

1. 默认按投诉时间倒序排列
2. 支持按风险等级优先排序、投诉时间正序、满意度评分升序/倒序排列

#### 分页功能

与数据管理中心页规则一致

#### 页面核心操作功能

1. 查询：多条件组合精准查询目标投诉分析数据
2. 查看详情：跳转至投诉分析详情页，查看全量AI分析结果、整改流程、回访记录
3. 编辑修正：手动修改AI生成的分类、根因、责任部门、风险等级，修正系统误差
4. 重新分析：触发AI智能体重新分析投诉内容，更新分析结果
5. 生成整改工单：单条/批量为待生成工单的投诉，一键生成整改工单，自动流转至整改闭环模块
6. 导出：导出筛选后的全量分析列表、AI根因分析结果，支持Excel格式
7. 批量操作：批量标记、批量生成工单、批量导出

#### 扩展功能

1. 高亮提醒：高风险、重复投诉、不满意整改的工单自动标红高亮
2. 分析日志：查看AI分析全流程记录、模型版本、人工修正记录

### 【MVP核心页面4：投诉分析详情页】

#### 页面核心功能

展示单条投诉全量信息、AI分析全过程、整改流程节点、满意度回访记录，支持全流程操作

#### 页面四大功能区域

1. 基础信息区
   - 展示内容：业主全量信息、投诉原始内容、400通话信息、满意度回访原始内容、关联工单号
   - 操作功能：编辑修改基础信息、重新关联满意度数据
2. AI智能分析结果区
   - 展示内容：投诉一级/二级分类、三层级根因分析结果、责任部门、风险等级、重复投诉判定结果
   - 操作功能：编辑修正分析结果、重新触发AI分析、打印分析报告
3. 整改闭环流程区
   - 展示内容：整改工单全信息、处理人、整改要求、整改时限、整改进度、整改提交内容、核验结果、全流程操作日志
   - 操作功能：生成整改工单、编辑工单信息、催办、退回整改、核验闭环
4. 满意度校验与复盘区
   - 展示内容：满意度评分、回访记录、不满意原因、整改效果AI判定结果、优化建议
   - 操作功能：手动补充回访记录、标记整改效果、触发二次整改

### 【MVP核心页面5：整改闭环智能管控页】

#### 页面核心功能

AI智能体自主管控整改工单全流程，追踪整改进度，处理预警任务，实现业务闭环

#### 列表全字段

| 字段名称 | 字段类型 | 枚举值/规则 |
| --- | --- | --- |
| 工单ID | 文本 | 系统自动生成，唯一标识，不可编辑 |
| 关联投诉工单号 | 文本 | 关联对应投诉工单，不可编辑 |
| 投诉问题摘要 | 文本 | 自动同步投诉核心内容，不可编辑 |
| 责任部门 | 枚举单选 | 同投诉分析页枚举值 |
| 整改要求 | 长文本 | AI自动生成，支持手动编辑 |
| 整改时限 | 时间 | 截止日期+时间，AI自动配置，支持手动编辑 |
| 处理人 | 文本 | 对应责任部门人员，必填 |
| 整改提交信息 | 组合字段 | 整改结果说明、整改凭证、提交时间 |
| 核验状态 | 枚举单选 | 【待核验、核验通过、核验不通过】 |
| 闭环状态 | 枚举单选 | 【未闭环、已闭环、逾期未闭环】 |
| 预警状态 | 枚举单选 | 【正常、即将逾期、已逾期】 |
| 满意度校验结果 | 枚举单选 | 【待校验、校验通过、校验不通过】 |

#### 筛选条件（全量可组合查询）

1. 责任部门（多选）
2. 核验状态（多选）
3. 闭环状态（多选）
4. 预警状态（多选）
5. 整改时限范围（起止时间）
6. 处理人、关联工单号（精准搜索）

#### 排序规则

1. 默认按预警状态优先排序（已逾期→即将逾期→正常）
2. 支持按整改时限升序、提交时间倒序排列

#### 分页功能

与数据管理中心页规则一致

#### 页面核心操作功能

1. 查询：多条件组合精准查询整改工单
2. 查看详情：跳转至工单详情页，查看全量信息与操作日志
3. 编辑：修改整改要求、整改时限、处理人、责任部门
4. 催办：对即将逾期/已逾期工单，一键发送催办通知给处理人
5. 核验：审核整改结果，核验通过则进入满意度校验环节；核验不通过则退回整改，重新设置整改时限
6. 闭环：满意度校验通过后，一键完成工单闭环
7. 导出：导出筛选后的工单列表、逾期预警清单、闭环完成清单，支持Excel格式
8. 批量操作：批量催办、批量导出、批量核验

#### 扩展功能

1. 自动预警：AI智能体实时监控工单时限，即将逾期/已逾期自动推送预警通知，无需人工触发
2. 自动升级：已逾期超3个工作日的工单，自动升级推送至部门负责人
3. 流程日志：全量记录工单从生成到闭环的每一步操作、操作人、操作时间，可追溯
4. 满意度自动校验：工单核验通过后，AI自动匹配对应400满意度回访数据，自动判定整改是否合格，不合格自动退回二次整改

### 【MVP核心页面6：数据可视化看板页】

#### 页面核心功能

以图表形式直观展示全量投诉核心数据，适配管理层决策与赛事路演演示，支持大屏全屏展示

#### 页面核心展示模块（按优先级排序）

1. 核心数据概览区（顶部）
   - 核心指标：投诉总量、环比增长率、整改完成率、平均闭环时长、满意度平均分、重复投诉率
   - 每个指标支持点击跳转至对应明细列表
2. 投诉分类分析区
   - 投诉一级分类占比饼图、二级分类TOP10排行柱状图
   - 支持按时间、部门筛选
3. 趋势分析区
   - 投诉量日/周/月趋势折线图、整改闭环时长趋势图、满意度走势折线图
4. 根因与责任分析区
   - 深层管理根因占比排行、责任部门投诉量与整改完成率对比柱状图
5. 区域热力分析区
   - 各项目/楼栋投诉量热力分布图

#### 页面操作功能

1. 时间筛选：支持按日、周、月、季度、自定义时间范围筛选数据
2. 部门筛选：支持按责任部门、项目筛选对应数据
3. 图表导出：支持单张图表导出为图片，对应明细数据导出为Excel
4. 全屏展示：支持全屏大屏展示，适配赛事路演演示场景
5. 钻取查询：点击图表指标，可跳转至对应明细数据列表

### 【扩展加分页面7：AI智能问答页】

#### 页面核心功能

实现自然语言交互，用户通过口语化提问，AI智能体自动查询数据、输出分析结论，是Agent核心能力展示页面

#### 页面核心功能

1. 问答输入：支持文字/语音输入，适配物业场景专属提问（示例：本月投诉最多的根因是什么？XX项目重复投诉率最高的问题是？工程部逾期工单有多少？）
2. 结果展示：AI自动输出自然语言结论+对应数据图表支撑，精准回应用户提问
3. 历史问答：展示历史查询记录，支持一键重新查询、复制结果
4. 问答导出：支持导出当前问答内容与对应数据结论

#### 扩展功能

- 常用问题快捷入口：预设高频提问按钮，一键点击快速查询
- 问答收藏：支持收藏高频查询问题，快速复用

### 【扩展加分页面8：自动化报告管理页】

#### 页面核心功能

AI自动生成标准化投诉治理分析报告，替代人工报告撰写工作

#### 列表全字段

| 字段名称 | 字段类型 | 枚举值/规则 |
| --- | --- | --- |
| 报告ID | 文本 | 系统自动生成，唯一标识 |
| 报告类型 | 枚举单选 | 【日报、周报、月报、季度报、自定义周期报告】 |
| 统计周期 | 时间范围 | 报告对应统计起止时间 |
| 生成时间 | 时间 | 系统自动记录 |
| 核心数据摘要 | 组合字段 | 投诉总量、整改完成率、满意度平均分、重复投诉率 |
| 生成状态 | 枚举单选 | 【生成中、已生成、生成失败】 |
| 操作人 | 文本 | 系统自动记录 |

#### 筛选条件

1. 报告类型（多选）
2. 统计周期范围
3. 生成状态（多选）

#### 页面核心操作功能

1. 手动生成：选择报告类型、统计周期，一键触发AI生成报告
2. 查看详情：在线查看完整报告，包含数据概览、分类分析、根因分析、整改情况、优化建议
3. 导出：支持导出PDF/Word格式报告
4. 删除：删除历史冗余报告
5. 自动生成配置：配置报告自动生成规则，每日/每周/月固定时间自动生成对应报告，推送至指定负责人

### 【基础配置页面9：系统管理配置页】

#### 子页面9.1：投诉分类配置页

| 字段名称 | 字段类型 | 枚举值/规则 |
| --- | --- | --- |
| 分类ID | 文本 | 系统自动生成，唯一标识 |
| 分类级别 | 枚举单选 | 【一级分类、二级分类】 |
| 分类名称 | 文本 | 必填，不可重复 |
| 上级分类 | 下拉选择 | 二级分类需绑定对应一级分类 |
| 排序序号 | 数字 | 用于列表展示排序 |
| 启用状态 | 枚举单选 | 【启用、禁用】 |

核心操作功能：新增、编辑、删除、启用/禁用、批量导入、导出

#### 子页面9.2：根因知识库配置页

| 字段名称 | 字段类型 | 枚举值/规则 |
| --- | --- | --- |
| 根因ID | 文本 | 系统自动生成，唯一标识 |
| 对应投诉分类 | 下拉选择 | 绑定对应一级/二级投诉分类 |
| 根因层级 | 枚举单选 | 【表层问题、直接原因、深层管理根因】 |
| 根因内容 | 长文本 | 必填，标准化根因描述 |
| 启用状态 | 枚举单选 | 【启用、禁用】 |
| 创建时间 | 时间 | 系统自动记录 |

核心操作功能：新增、编辑、删除、启用/禁用、批量导入、导出、模型同步（更新至AI根因分析模型）

#### 子页面9.3：部门与用户权限配置页

| 字段名称 | 字段类型 | 枚举值/规则 |
| --- | --- | --- |
| 用户ID | 文本 | 系统自动生成，唯一标识 |
| 工号 | 文本 | 必填，唯一 |
| 姓名 | 文本 | 必填 |
| 所属部门 | 下拉选择 | 对应责任部门枚举值 |
| 角色 | 枚举单选 | 【客服专员、执行人员、部门管理者、集团管理层、系统管理员】 |
| 权限范围 | 复选框 | 对应系统页面与操作权限 |
| 账号状态 | 枚举单选 | 【启用、禁用】 |

核心操作功能：新增、编辑、删除、启用/禁用、重置密码、权限批量配置

#### 子页面9.4：智能体规则配置页

核心配置项：

1. 整改时限规则配置：不同风险等级、不同分类投诉的默认整改时限
2. 预警规则配置：提前预警时间、预警推送对象、升级规则
3. 根因分析模型配置：模型版本、知识库同步规则、自学习迭代开关
4. 满意度校验规则配置：满意度评分阈值、不合格自动退回规则

## 八、全量枚举值汇总（标准化无遗漏）

### 1. 数据类型枚举

业主进线投诉数据、400外呼满意度数据、外呼回访考核数据

### 2. 数据处理状态枚举

待清洗、已清洗、已分析、已关联、匹配失败

### 3. 投诉一级分类枚举

设施设备类、物业服务类、收费争议类、环境绿化类、安全管理类、邻里纠纷类、其他类

### 4. 投诉二级分类枚举（物业行业标准化）

| 一级分类 | 二级分类枚举值 |
| --- | --- |
| 设施设备类 | 电梯故障、门禁系统故障、供水故障、供电故障、消防设备故障、公共照明故障、排水系统故障、空调系统故障、其他设备故障 |
| 物业服务类 | 客服服务态度差、报修响应不及时、装修管理违规、快递服务问题、社区活动问题、信息公示不及时、其他服务问题 |
| 收费争议类 | 物业费异议、停车费异议、水电费异议、增值服务费异议、退费问题、其他收费争议 |
| 环境绿化类 | 保洁不到位、垃圾清运不及时、消杀不到位、绿化养护差、公共区域异味、其他环境问题 |
| 安全管理类 | 外来人员管理不严、车辆管理混乱、监控设备故障、巡逻不到位、消防通道堵塞、高空抛物、其他安全问题 |
| 邻里纠纷类 | 噪音扰民、占用公共区域、宠物扰民、其他邻里纠纷 |
| 其他类 | 地产遗留问题、其他无法归类问题 |

### 5. 责任部门枚举

工程部、保洁部、客服部、秩序部、财务部、项目管理部、地产维保部

### 6. 投诉风险等级枚举

高、中、低

### 7. 整改状态枚举

待生成工单、待整改、整改中、待核验、已闭环、整改失败

### 8. 满意度结果枚举

满意、基本满意、不满意

### 9. 核验状态枚举

待核验、核验通过、核验不通过

### 10. 闭环状态枚举

未闭环、已闭环、逾期未闭环

### 11. 预警状态枚举

正常、即将逾期、已逾期

### 12. 根因层级枚举

表层问题、直接原因、深层管理根因

### 13. 报告类型枚举

日报、周报、月报、季度报、自定义周期报告

### 14. 分类级别枚举

一级分类、二级分类

### 15. 用户角色枚举

客服专员、执行人员、部门管理者、集团管理层、系统管理员

### 16. 账号/分类启用状态枚举

启用、禁用

## 九、MVP验收与演示脚本（交付研发必备）

### 9.1 MVP验收范围（必须可演示）

1. 登录与权限校验（页面1）
2. 数据管理中心：导入、清洗、关联、查看详情、编辑修正、导出（页面2）
3. 投诉AI分析列表：查询、查看详情、编辑修正、重新分析、生成整改工单（页面3）
4. 投诉分析详情：查看全量信息、编辑修正、生成/查看工单、催办、核验、退回、闭环（页面4）
5. 整改闭环智能管控：预警、催办、核验、满意度校验、闭环（页面5）
6. 数据可视化看板：核心指标、筛选、钻取、导出（页面6）

### 9.2 端到端演示脚本（建议作为联调验收用例）

#### 场景A：标准闭环（投诉→整改→核验→满意度→闭环）

前置条件：已配置部门与用户、投诉分类、根因知识库、整改时限/预警/满意度校验规则。

1. 客服专员导入“业主进线投诉数据”1条（工单号唯一）
2. 在数据管理中心对该数据执行“一键清洗”，状态从“待清洗”变更为“已清洗”
3. 在投诉AI分析列表触发“重新分析/分析”，生成一级/二级分类、三层根因、责任部门、风险等级、重复投诉标记
4. 点击“生成整改工单”，整改状态进入“待整改”，工单进入整改闭环管控列表
5. 部门执行人员提交整改结果与凭证，整改状态进入“待核验”，核验状态为“待核验”
6. 部门管理者执行核验：核验通过→满意度校验结果进入“待校验”
7. 导入对应的“400外呼满意度数据”，系统自动关联并触发满意度校验：校验通过→闭环状态“已闭环”，整改状态“已闭环”
8. 看板指标同步更新：整改完成率、平均闭环时长、满意度均分等

验收点：每一步均产生审计日志；工单不可重复生成；状态流转符合状态机定义。

#### 场景B：关联失败修复（满意度数据匹配失败→人工修复→补齐校验）

1. 导入投诉数据（工单号存在）
2. 导入满意度数据（工单号缺失或错误）→数据处理状态“匹配失败”
3. 客服专员在数据管理中心对满意度数据补录/修正“关联工单号”
4. 执行“自动关联”→状态进入“已关联”
5. 触发满意度校验并落库校验结果

验收点：匹配失败可筛选、可定位；修复后可追溯“修复前后值”。

#### 场景C：核验不通过退回 + 二次整改 + 满意度不通过自动退回

1. 工单进入“待核验”
2. 核验不通过→进入“整改中”，核验状态“核验不通过”，并产生退回原因与新的整改时限
3. 再次提交后核验通过→满意度校验进入“待校验”
4. 导入满意度数据后校验不通过→系统自动退回二次整改（整改状态回到“整改中”），并记录“不通过原因与规则命中”

验收点：退回原因必填；退回次数可统计；自动退回可在日志中追溯触发规则。

## 十、数据模型与数据字典（研发实现口径）

### 10.1 核心实体清单

1. 数据记录（DataRecord）：承载投诉/满意度原始数据、清洗结果、关联状态
2. 投诉分析（ComplaintAnalysis）：承载AI分析输出、人工修正结果、模型版本、置信度等
3. 整改工单（RectificationWorkOrder）：承载整改要求、处理人、时限、预警、核验、满意度校验与闭环
4. 分类字典（ComplaintCategory）：一级/二级分类配置
5. 根因知识库（RootCauseKB）：根因条目与层级配置
6. 组织与账号（Department/User）：部门、角色、权限范围、账号状态
7. 规则配置（AgentRuleConfig）：时限/预警/升级/满意度阈值/置信度阈值/关联规则等
8. 审计日志（AuditLog）：关键操作留痕
9. 通知记录（NotificationLog）：催办/预警/升级/系统通知

### 10.2 主键与编号规则

1. 数据ID/分析ID/工单ID/分类ID/根因ID/用户ID/报告ID：系统生成，保证全局唯一（建议UUID/雪花ID）
2. 关联工单号：业务唯一编号，用于投诉与满意度的主关联键；在同一业务域内必须唯一

### 10.3 关键字段类型与校验规则（最小必备）

| 字段 | 类型 | 必填 | 校验/格式 | 说明 |
| --- | --- | --- | --- | --- |
| 关联工单号 | string | 是 | 1-64字符；不允许全空白 | 投诉与满意度主关联键 |
| 数据类型 | enum | 是 | 业主进线投诉数据/400外呼满意度数据/外呼回访考核数据 | 决定后续字段校验规则 |
| 通话时间/回访时间 | datetime | 是 | yyyy-MM-dd HH:mm:ss | 用于排序、趋势统计 |
| 通话时长 | int | 否 | >=0，单位秒 | 可空但不可为负 |
| 接待坐席/回访坐席 | string | 是 | 1-64字符 | 坐席标识或姓名 |
| 楼栋房号 | string | 否 | 1-128字符 | 支持模糊检索 |
| 联系电话 | string | 否 | 手机号格式校验（可配置） | 涉及脱敏展示 |
| 原始数据内容 | text | 是 | 最小长度>0 | 必须保留原文，禁止覆盖 |
| 满意度评分 | int | 否 | 1-10 | 仅满意度数据使用 |

### 10.4 Excel导入模板（列定义）

模板要求：首行为表头；支持批量导入；导入后必须返回成功条数、失败条数、失败原因明细（含行号）。

#### 模板A：业主进线投诉数据（DataRecord）

| 列名 | 必填 | 说明 |
| --- | --- | --- |
| 数据类型 | 是 | 固定为“业主进线投诉数据” |
| 关联工单号 | 是 | 业务唯一 |
| 业主姓名 | 否 | 可空 |
| 楼栋房号 | 否 | 可空 |
| 联系电话 | 否 | 可空，建议校验 |
| 通话时间 | 是 | yyyy-MM-dd HH:mm:ss |
| 通话时长（秒） | 否 | 非负整数 |
| 接待坐席 | 是 | 文本 |
| 原始数据内容 | 是 | 投诉原始描述 |

#### 模板B：400外呼满意度数据（DataRecord）

| 列名 | 必填 | 说明 |
| --- | --- | --- |
| 数据类型 | 是 | 固定为“400外呼满意度数据” |
| 关联工单号 | 是 | 与投诉工单号一致；缺失则导入成功但标记“匹配失败” |
| 业主姓名 | 否 | 可空 |
| 楼栋房号 | 否 | 可空 |
| 联系电话 | 否 | 可空，建议校验 |
| 回访时间 | 是 | yyyy-MM-dd HH:mm:ss |
| 回访坐席 | 是 | 文本 |
| 满意度评分（1-10） | 是 | 整数1-10 |
| 原始数据内容 | 是 | 回访原始记录 |

#### 模板C：外呼回访考核数据（DataRecord）

数据来源示例：`原始数据数据底稿.xlsx`（sheet：考核项目）。

| 列名 | 必填 | 说明 |
| --- | --- | --- |
| 数据类型 | 是 | 固定为“外呼回访考核数据” |
| 地区公司 | 是 | 文本 |
| 项目名称 | 是 | 文本 |
| 楼栋号码 | 是 | 文本；可能存在脱敏符号 |
| 是否过保 | 否 | 日期/文本（以原始数据为准） |
| 姓名 | 是 | 文本；可能存在脱敏符号 |
| 电话号码 | 是 | 文本；可能存在脱敏符号 |
| 性别 | 否 | 枚举（男/女/空） |
| 任务批次 | 是 | 文本 |
| 分配人 | 是 | 文本 |
| 分配时间 | 是 | yyyy-MM-dd HH:mm:ss（Excel序列需转换） |
| 拨打时间 | 是 | yyyy-MM-dd HH:mm:ss（Excel序列需转换） |
| 状态 | 是 | 文本（当前数据为“已完成”） |
| 业务结果 | 是 | 文本（如：已配合调查/不配合调查/无人接/拒接/无法接通等） |
| 是否接通 | 是 | 枚举（是/否） |
| 是否有效 | 是 | 枚举（有/无）；需对脏值做映射 |
| 非常住可用 | 是 | 枚举（可/不可/其他，以原始数据为准） |
| 首轮评价 | 否 | 枚举（满意/一般/不满意）；通常仅“已配合调查”有值 |
| 居住情况 | 否 | 枚举（常住/住过/未入住/不常住/已出租等） |
| 400类 | 否 | 预留字段（当前数据缺失率极高） |
| 一般问题 | 否 | 长文本；可能含换行与控制符 |
| 管家服务 | 否 | 枚举（满意/一般/不满意/未评价） |
| 安保服务 | 否 | 枚举（满意/一般/不满意/未评价） |
| 环境卫生 | 否 | 枚举（满意/一般/不满意/未评价） |
| 公区维修 | 否 | 枚举（满意/一般/不满意/未评价） |
| 调查备注问题 | 否 | 长文本 |

### 10.5 导入校验与错误处理策略

1. 行级校验失败：该行失败，其他行可继续导入；返回失败行号与原因
2. 必填字段缺失：明确提示缺失列名/行号
3. 枚举值非法：提示合法枚举集合
4. 关联工单号重复：按规则配置处理（默认拒绝并提示冲突行；可配置为覆盖更新）

### 10.6 外呼回访考核数据清洗与标准化规则（基于真实底稿统计）

#### 10.6.1 业务分层口径（避免统计失真）

1. “首轮评价/居住情况/分项评价/一般问题/调查备注问题”等字段存在明显的条件缺失，应以“业务结果”为分层前提进行统计与建模
2. “业务结果=已配合调查”是满意度与分项评价的主要有效子集，适用于满意度反向校验与问题主题分析

#### 10.6.2 时间字段转换

1. 分配时间、拨打时间在底稿中为Excel序列（含小数），导入时必须统一转换为 yyyy-MM-dd HH:mm:ss
2. 转换失败的行标记为导入失败并提示具体行号与列名

#### 10.6.3 枚举标准化（脏值映射）

1. 是否有效：将“无（无）/有（无）/有（删）”等归一为“无/有”，原始值保留至清洗日志
2. 文本字段：去除 `_x000D_` 等控制符；统一换行符；保留原文与清洗后文本两份

#### 10.6.4 一致性稽核（导入后自动生成稽核结果）

1. 拨打时间应大于等于分配时间，违反则标记“时间异常”
2. 是否接通与业务结果应满足一致性校验（允许配置豁免清单），违反则标记“结果异常”
3. 400类字段当前缺失率极高，MVP阶段不作为必填、不作为核心统计口径

#### 10.6.5 枚举字典化与映射表（MVP默认标准）

1. 是否接通枚举：是 / 否
2. 是否有效枚举：有 / 无
3. 首轮评价枚举：满意 / 一般 / 不满意
4. 分项评价枚举：满意 / 一般 / 不满意 / 未评价
5. 业务结果字典（用于筛选、看板与一致性校验，允许扩展）：
   - 已配合调查
   - 不配合调查
   - 无人接
   - 正在通话中
   - 无法接通
   - 拒接
   - 停机
   - 空号
   - 错误号码
   - 因方言无法沟通

脏值处理：导入时若命中未知业务结果，则记录为“其他”并在清洗日志中保留原值；是否接通/是否有效等枚举若出现括号扩展值，按映射表归一。

## 十一、关联匹配、重复投诉与口径规则（可计算化）

### 11.1 投诉-满意度关联匹配规则

1. 主规则：以“关联工单号”进行一对一匹配
2. 匹配失败：数据处理状态标记为“匹配失败”，进入“匹配失败处理”流程，允许人工修正工单号后重新关联
3. 允许多次回访：同一工单号存在多条满意度数据时，默认取最新回访时间的一条作为“当前满意度”，其他作为历史记录保留

### 11.3 外呼回访考核数据关联规则（现实约束）

1. 外呼回访考核数据存在电话与楼栋号脱敏（如 `139****1277`、`***`），默认不承诺与投诉工单的100%自动关联
2. 默认策略：该类数据先进入“满意度与服务评价分析池”，用于项目/地区/批次/分配人维度的看板与问题主题分析
3. 若需要纳入闭环链路，必须在导入模板中补充至少一种可关联主键（如：内部业主ID/房产ID/工单号/未脱敏手机号的内部映射），并在规则配置中启用对应的匹配策略

### 11.4 满意度数据源优先级与冲突处理（MVP默认规则）

1. 优先级：400外呼满意度数据 > 外呼回访考核数据
2. 同一工单若同时存在两类满意度：用于“工单满意度校验”的默认取400外呼满意度；外呼回访考核数据仍可进入看板统计并标记来源
3. 无法关联到工单的外呼回访考核数据：进入“满意度与服务评价分析池”，不影响工单闭环状态
4. 规则可配置：允许在系统管理的“满意度校验规则配置”中切换优先级与是否启用外呼回访考核数据参与闭环校验

### 11.2 重复投诉判定规则

1. 判定维度：同一联系电话（如缺失则使用楼栋房号）+同一投诉二级分类
2. 时间窗：30天内出现2次及以上投诉
3. 配置化：时间窗天数、阈值次数可在“智能体规则配置”中调整

## 十二、流程与状态机（研发联调口径）

### 12.1 数据处理状态机（DataRecord）

1. 待清洗：数据导入成功后默认进入
2. 已清洗：执行清洗成功后进入
3. 已分析：对投诉数据执行AI分析成功后进入（仅投诉数据）
4. 已关联：投诉与满意度匹配成功后进入
5. 匹配失败：自动关联失败进入，可人工修复后回到“已关联”

### 12.2 整改工单主状态（整改状态）与流转

| 当前整改状态 | 触发动作 | 下一整改状态 |
| --- | --- | --- |
| 待生成工单 | 生成整改工单 | 待整改 |
| 待整改 | 接单/开始处理 | 整改中 |
| 整改中 | 提交整改 | 待核验 |
| 待核验 | 核验通过 | 待核验（保持） |
| 待核验 | 核验不通过/退回整改 | 整改中 |
| 待核验 | 满意度校验通过并闭环 | 已闭环 |
| 任意 | 管理员强制终止 | 整改失败 |

说明：核验通过后进入满意度校验阶段，是否允许“无满意度数据直接闭环”由规则配置决定，默认不允许。

### 12.3 子状态定义（核验/闭环/预警/满意度）

1. 核验状态：待核验 / 核验通过 / 核验不通过  
2. 闭环状态：未闭环 / 已闭环 / 逾期未闭环  
3. 预警状态：正常 / 即将逾期 / 已逾期  
4. 满意度校验结果：待校验 / 校验通过 / 校验不通过

### 12.4 预警与升级规则（可配置）

1. 即将逾期：当前时间到整改时限剩余时间小于等于预警阈值（默认24小时）进入
2. 已逾期：当前时间超过整改时限进入
3. 自动升级：已逾期超过N个工作日（默认3个工作日）自动升级至部门负责人
4. 工作日口径：由系统维护节假日历（如未配置，则按自然日计算并在界面提示）

### 12.5 满意度结果映射规则（可配置）

1. 满意度评分9-10：满意
2. 满意度评分7-8：基本满意
3. 满意度评分1-6：不满意

满意度阈值与“校验不通过自动退回”规则在“智能体规则配置”中可调整。

### 12.6 强制闭环规则（MVP默认行为）

1. 默认不允许在无任何满意度数据的情况下直接闭环
2. 管理员可执行“强制闭环”，必须填写原因，并写入审计日志
3. 强制闭环后的满意度校验结果显示为“待校验”，并在工单详情中标记“强制闭环”

## 十三、权限矩阵与审计（交付研发必须明确）

### 13.1 角色数据范围（默认口径）

1. 客服专员：可查看全量投诉/满意度数据与分析结果；可导入、清洗、关联、修正；不可进行核验与闭环
2. 部门执行人员：仅查看本部门工单；可提交整改；不可调整责任部门与规则配置
3. 项目/部门管理者：查看本部门工单与分析结果；可核验、退回、改派处理人、催办；可处理升级预警
4. 集团管理层：查看全量看板、分析与报告；默认只读；导出权限可配置
5. 系统管理员：全量权限，含字典/规则/权限配置

### 13.2 页面与操作权限（最小矩阵）

| 页面/功能 | 客服专员 | 部门执行人员 | 部门管理者 | 集团管理层 | 系统管理员 |
| --- | --- | --- | --- | --- | --- |
| 登录 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 数据导入/删除/导出 | ✅ | ❌ | ❌ | 可配置 | ✅ |
| 数据清洗/关联/编辑修正 | ✅ | ❌ | ❌ | ❌ | ✅ |
| 投诉分析查看 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 投诉分析编辑修正/重新分析 | ✅ | ❌ | ✅ | ❌ | ✅ |
| 生成整改工单 | ✅ | ❌ | ✅ | ❌ | ✅ |
| 工单提交整改 | ❌ | ✅ | ❌ | ❌ | ✅ |
| 催办/升级处理 | ❌ | ✅（仅对本人） | ✅ | ❌ | ✅ |
| 核验/退回/闭环 | ❌ | ❌ | ✅ | ❌ | ✅ |
| 看板与报告查看 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 系统配置（分类/知识库/权限/规则） | ❌ | ❌ | ❌ | ❌ | ✅ |

### 13.3 审计日志（必做）

审计对象：数据导入/删除、清洗、关联、AI重新分析、分类/根因/责任部门/风险等级人工修正、生成工单、改派处理人、修改整改时限、提交整改、核验通过/不通过、满意度校验、自动退回、闭环、导出、配置变更。

日志字段：日志ID、实体类型、实体ID、操作类型、操作人、操作时间、变更前值、变更后值、备注/原因、来源（页面/接口/定时任务）。

### 13.4 脱敏与明文查看（MVP默认规则）

1. 电话号码、楼栋房号、通话/回访原文属于敏感信息，页面展示与导出默认脱敏
2. 是否允许明文查看：仅系统管理员可配置开启；开启后仅对具备“明文查看权限”的角色生效，并写入审计日志

## 十四、AI接口契约与人机协作（研发对接口径）

### 14.1 投诉AI分析接口（示例字段约定）

输入（ComplaintAnalysisRequest）：

```json
{
  "workOrderNo": "string",
  "complaintText": "string",
  "callTime": "yyyy-MM-dd HH:mm:ss",
  "owner": {
    "name": "string",
    "phone": "string",
    "room": "string"
  }
}
```

输出（ComplaintAnalysisResponse）：

```json
{
  "analysisId": "string",
  "categoryLevel1": "设施设备类",
  "categoryLevel2": "电梯故障",
  "rootCause": {
    "surfaceProblem": "string",
    "directCause": "string",
    "deepManagementRootCause": "string"
  },
  "responsibleDept": "工程部",
  "riskLevel": "高",
  "isRepeatedComplaint": true,
  "confidence": 0.0,
  "evidenceSnippets": [
    {
      "text": "string",
      "reason": "string"
    }
  ],
  "suggestedRectification": {
    "requirement": "string",
    "deadline": "yyyy-MM-dd HH:mm:ss"
  },
  "modelVersion": "string",
  "analyzedAt": "yyyy-MM-dd HH:mm:ss"
}
```

### 14.2 置信度与人工介入规则（可配置）

1. 置信度低于阈值时：整改工单默认进入“待生成工单”，并标记“需人工确认”
2. 置信度高于阈值时：允许一键批量生成整改工单
3. 人工修正后的策略：默认不被自动覆盖；仅在用户明确触发“重新分析并覆盖”时更新

默认阈值：0.7（可在规则配置中调整）。

### 14.3 满意度校验接口（示例字段约定）

输入：工单号、核验通过时间、满意度评分、回访原文

输出：满意度结果（满意/基本满意/不满意）、校验结果（通过/不通过）、不通过原因标签、触发规则命中信息。

## 十五、指标口径与导出规范（看板/报告一致性）

### 15.1 核心指标口径

1. 投诉总量：统计周期内投诉数据条数（按关联工单号去重）
2. 整改完成率：统计周期内“已闭环”工单数 / 统计周期内应闭环工单数
3. 平均闭环时长：已闭环工单的（闭环时间-生成工单时间）平均值
4. 满意度平均分：统计周期内已关联满意度记录的评分平均值
5. 重复投诉率：重复投诉标记为“是”的投诉数 / 投诉总量
6. 环比增长率：（本期投诉总量-上期投诉总量）/上期投诉总量

### 15.3 外呼运营与服务评价指标（来源：外呼回访考核数据）

说明：本组指标默认按“外呼回访考核数据”统计，且服务评价类指标默认限定在“业务结果=已配合调查”子集。

1. 外呼触达率：是否接通=是 / 外呼总量
2. 外呼配合率：业务结果=已配合调查 / 外呼总量
3. 首轮评价分布（子集）：满意/一般/不满意占比
4. 分项服务评价分布（子集）：管家服务/安保服务/环境卫生/公区维修 的满意/一般/不满意/未评价占比
5. 问题主题TOP（子集）：一般问题与调查备注问题的主题统计（去控制符、按规则抽取“问题类型：描述”优先）

### 15.2 导出规范

1. 列表导出字段：与列表展示字段一致，并包含关键状态字段与时间字段
2. 导出权限：与角色权限矩阵一致，集团管理层导出默认关闭（可配置）
3. 脱敏：导出默认对联系电话脱敏（如需全量明文须管理员授权）
4. 审计：每次导出必须写入审计日志

导出补充：服务评价与问题主题导出必须携带“数据来源类型”字段，便于追溯。

## 十六、异常与边界场景清单（开发联调必测）

1. 导入：缺列/缺必填、枚举非法、时间格式非法、超大文件、部分成功部分失败
2. 关联：投诉无满意度、满意度无投诉、同工单多条满意度、工单号冲突
3. 分析：AI超时/失败、重复点击重新分析、人工修正后再分析覆盖策略
4. 工单：重复生成、改派处理人、修改时限导致预警重算、核验退回多次、强制终止
5. 通知：催办/预警/升级发送失败、重复发送频控、接收人账号禁用
6. 权限：越权访问接口、导出越权、跨部门数据隔离

### 16.1 异常场景的系统表现（MVP默认规则）

1. 导入失败：支持行级失败并返回失败行号与原因；成功行可落库
2. 关联失败：进入“匹配失败”状态并可筛选；支持人工修复后重新关联
3. 一致性稽核异常：默认不阻断导入，但在清洗日志中标记异常类型；可配置为阻断
4. AI分析失败：保留原始数据与历史分析版本，页面提示失败原因并支持重试

## 十八、通知与附件（MVP默认实现约束）

### 18.1 通知渠道（MVP默认）

1. 催办/预警/升级：MVP默认使用站内通知（通知记录可追溯）
2. 扩展渠道：支持对接企业微信/短信/邮件（作为加分项配置）
3. 失败重试：通知发送失败需记录失败原因并支持重试

### 18.2 整改凭证附件规范（MVP默认）

1. 支持类型：图片（jpg/png/webp）与文件（pdf/doc/docx/xlsx）
2. 单文件大小上限：20MB
3. 单工单附件数量上限：20
4. 下载鉴权：仅有权限的角色可下载；下载行为写入审计日志

## 十七、非功能与合规要求（必须写入研发实现约束）

1. 隐私与脱敏：联系电话、通话内容、回访原文均为敏感信息；页面展示与导出需支持脱敏；访问需审计
2. 安全：密码加密存储与传输；登录失败次数限制与账号锁定；会话过期与权限后端校验
3. 可用性：关键任务（预警、升级、校验）需具备失败重试与幂等控制
4. 可追溯：所有关键操作必须可追溯（审计日志、流程日志、规则命中信息）

## 十九、研发启动必确认项（产品决策清单）

| 决策项 | 默认建议（MVP） | 影响范围 | 不确认的风险 |
| --- | --- | --- | --- |
| 外呼回访考核数据是否参与“工单满意度闭环校验” | 不参与，仅进入“满意度与服务评价分析池” | 关联规则、闭环状态机、校验逻辑 | 研发实现方向不确定，易引发重构 |
| 满意度数据源优先级 | 400外呼满意度 > 外呼回访考核数据 | 校验结果、看板口径、导出追溯 | 多来源冲突导致数据不一致 |
| 外呼回访考核数据闭环主键方案 | 默认不强绑定；如需闭环必须补充：工单号/内部业主ID/房产ID/映射表 | 数据模型、导入模板、匹配策略、权限与审计 | 无法达成闭环承诺或关联准确率偏低 |
| 通知渠道（催办/预警/升级） | 站内通知 | 通知模块、权限、审计、可用性 | 渠道变更会引起联调与测试返工 |
| 附件存储方案（整改凭证） | 先按通用文件存储实现，保持可替换（本地/对象存储由技术方案确定） | 上传下载、鉴权、审计、容量与成本 | 影响部署方式与运维成本，后期迁移风险 |
| 工作日历口径 | 未配置节假日历时按自然日，并在页面提示 | 逾期/升级计算 | 升级与逾期计算口径争议 |
| 账号与组织数据来源 | MVP手工维护（系统管理配置页），后续可对接组织系统 | 登录、权限、数据范围 | 对接方式不确定导致权限改造 |
